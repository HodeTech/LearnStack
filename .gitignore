# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/*
# Shared workspace recommendations + cross-language editor settings are
# committed; per-user state (launch.json, history, etc.) stays gitignored.
!.vscode/extensions.json
!.vscode/settings.json
*.swp

# .NET
bin/
obj/
TestResults/
*.user
*.suo
*.rsuser

# Node
node_modules/
.next/
dist/
build/
coverage/

# Environment — track only the *.example templates, never real envs
.env
.env.*
!.env.example
!.env.local.example
!*.example

# Logs
*.log
logs/

# Local infrastructure data
.data/
seaweedfs-data/
postgres-data/
valkey-data/

# Local-only analyses (not shared via git)
docs/analysis/

