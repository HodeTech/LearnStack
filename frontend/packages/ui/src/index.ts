// Design-system primitives extracted from apps/web only when duplication is real
// (ADR-0009 § Decision). Empty for now — apps/web owns its own
// components until a second consumer appears.
export {};
